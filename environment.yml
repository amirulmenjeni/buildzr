name: buildzr-dev
channels:
  - conda-forge
dependencies:
  - python==3.8
  - pytest
  - ipython
  - pyhumps
  - mypy
  - pytest-mypy
  - pip
  - pip:
      - -e .
prefix: /opt/conda/envs/buildzr-dev
